<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{blog.user.name}} on {{format_date date_created}}
        </div>
        <div class="text">
            {{comment}}
        </div>
    </section>
    {{/each}}
</div>